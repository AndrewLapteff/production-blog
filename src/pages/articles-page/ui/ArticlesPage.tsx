import s from './ArticlesPage.module.scss'
import { classNames } from 'shared/lib'

const ArticlesPage = () => {
  return <div className={classNames(s.articlepage)}></div>
}

export default ArticlesPage
